/**
 * Water Quality feature: tank data and averages (static display; can be wired to API later).
 */
export function init() {
  // Static page; no setup required. Sync from dashboard sensors is done in app.js updateCard.
}
