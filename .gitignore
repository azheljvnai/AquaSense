# Dependencies
node_modules/

# Environment (private APIs and secrets)
.env

# Firebase service account key (never commit this)
serviceAccountKey.json
*serviceAccount*.json

# OS / editor
.DS_Store
Thumbs.db
*.log
.idea
.vscode

# Firebase CLI deploy cache (regenerated on deploy)
.firebase/

# Optional
dist/
build/
*.local
