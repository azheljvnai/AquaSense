# Implementation Plan

- [x] 1. Write bug condition exploration test
  - **Property 1: Bug Condition** - Historical Data Cards Match Dashboard Structure
  - **CRITICAL**: This test MUST FAIL on unfixed code - failure confirms the bug exists
  - **DO NOT attempt to fix the test or the code when it fails**
  - **NOTE**: This test encodes the expected behavior - it will validate the fix when it passes after implementation
  - **GOAL**: Surface counterexamples that demonstrate the bug exists
  - **Scoped PBT Approach**: Scope the property to concrete failing cases - Historical Data page cards with wrong structure
  - Test that Historical Data page cards have class "metric-card scard" (not "hist-stat-card")
  - Test that Historical Data page cards include sparkline SVG elements with IDs "sp-temp", "sp-ph", "sp-do", "sp-turb"
  - Test that Historical Data page card icons use class "icon-20" (not "icon-16")
  - Test that Historical Data page cards are ordered: Temperature, pH, DO, Turbidity (not pH, DO, Turbidity, Temperature)
  - Test that Historical Data page cards use element IDs "v-{metric}" and "b-{metric}" (not "hstat-{metric}-avg" and "hstat-{metric}-badge")
  - Run test on UNFIXED code
  - **EXPECTED OUTCOME**: Test FAILS (this is correct - it proves the bug exists)
  - Document counterexamples found:
    - Cards use "hist-stat-card" class instead of "metric-card scard"
    - Cards missing sparkline SVG elements
    - Cards use "icon-16" instead of "icon-20"
    - Cards in wrong order (pH, DO, Turbidity, Temperature)
    - Cards use old element IDs (hstat-{metric}-avg, hstat-{metric}-badge)
  - Mark task complete when test is written, run, and failure is documented
  - _Requirements: 2.1, 2.2, 2.3, 2.4, 2.5_

- [x] 2. Write preservation property tests (BEFORE implementing fix)
  - **Property 2: Preservation** - Dashboard and Statistical Calculations Unchanged
  - **IMPORTANT**: Follow observation-first methodology
  - Observe behavior on UNFIXED code for non-buggy inputs (Dashboard page, statistical calculations)
  - Write property-based tests capturing observed behavior patterns:
    - Dashboard page cards render with "metric-card scard" structure
    - Historical Data page calculates correct average, min, max statistics for sensor readings
    - Historical Data page updates badges based on threshold values (Normal/Warning/Critical)
    - Both pages use consistent color scheme (Temperature=red, pH=blue, DO=green, Turbidity=yellow)
    - Historical Data page updates cards when date range or thresholds change
  - Property-based testing generates many test cases for stronger guarantees
  - Run tests on UNFIXED code
  - **EXPECTED OUTCOME**: Tests PASS (this confirms baseline behavior to preserve)
  - Mark task complete when tests are written, run, and passing on unfixed code
  - _Requirements: 3.1, 3.2, 3.3, 3.4, 3.5_

- [x] 3. Fix Historical Data card structure to match Dashboard

  - [x] 3.1 Update HTML structure in public/index.html
    - Replace the four hist-stat-card divs (lines 305-340) with metric-card scard divs matching Dashboard structure
    - Change class from "hist-stat-card" to "metric-card scard {metric-class}" (temp-card, ph-card, do-card, turb-card)
    - Change "hist-stat-icon" to "metric-icon" with icon-20 instead of icon-16
    - Remove "hist-stat-body" wrapper div
    - Change "hist-stat-label" to "scard-label"
    - Change "hist-stat-val" to "scard-val" with ID "v-{metric}"
    - Change "hist-stat-badge" to "scard-badge" with ID "b-{metric}"
    - Remove "hist-stat-range" div (min-max display)
    - Add `<svg class="spark" id="sp-{metric}">` elements after scard-val
    - Reorder cards to match Dashboard: Temperature, pH, DO, Turbidity
    - _Bug_Condition: isBugCondition(input) where input.page == 'historical-data' AND cardStructure(input.page) != cardStructure('dashboard')_
    - _Expected_Behavior: cardStructure(result) == cardStructure('dashboard') AND hasSparklines(result) == true AND iconSize(result) == 20 AND cardOrder(result) == ['temp', 'ph', 'do', 'turb']_
    - _Preservation: Dashboard card structure, statistical calculations, threshold-based badge updates, color schemes_
    - _Requirements: 2.1, 2.2, 2.3, 2.4, 2.5, 3.1, 3.2, 3.3, 3.4, 3.5_

  - [x] 3.2 Update JavaScript selectors in public/js/features/historical-data.js
    - Modify updateStatCards() function to target new element IDs
    - Change `getElementById('hstat-${k}-avg')` to `getElementById('v-${k}')`
    - Change `getElementById('hstat-${k}-badge')` to `getElementById('b-${k}')`
    - Remove references to `hstat-${k}-min` and `hstat-${k}-max` elements (no longer displayed in cards)
    - Preserve statistical calculation logic (avg, min, max) even though min/max are not displayed in cards
    - _Bug_Condition: isBugCondition(input) where input.page == 'historical-data'_
    - _Expected_Behavior: updateStatCards() correctly updates new element IDs (v-{metric}, b-{metric})_
    - _Preservation: Statistical calculations, threshold-based badge updates_
    - _Requirements: 2.1, 3.2, 3.3, 3.5_

  - [x] 3.3 Verify bug condition exploration test now passes
    - **Property 1: Expected Behavior** - Historical Data Cards Match Dashboard Structure
    - **IMPORTANT**: Re-run the SAME test from task 1 - do NOT write a new test
    - The test from task 1 encodes the expected behavior
    - When this test passes, it confirms the expected behavior is satisfied
    - Run bug condition exploration test from step 1
    - **EXPECTED OUTCOME**: Test PASSES (confirms bug is fixed)
    - Verify Historical Data cards now have "metric-card scard" class
    - Verify Historical Data cards include sparkline SVG elements
    - Verify Historical Data cards use "icon-20" class
    - Verify Historical Data cards are ordered: Temperature, pH, DO, Turbidity
    - Verify Historical Data cards use element IDs "v-{metric}" and "b-{metric}"
    - _Requirements: 2.1, 2.2, 2.3, 2.4, 2.5_

  - [x] 3.4 Verify preservation tests still pass
    - **Property 2: Preservation** - Dashboard and Statistical Calculations Unchanged
    - **IMPORTANT**: Re-run the SAME tests from task 2 - do NOT write new tests
    - Run preservation property tests from step 2
    - **EXPECTED OUTCOME**: Tests PASS (confirms no regressions)
    - Confirm Dashboard cards still render correctly
    - Confirm statistical calculations still work correctly
    - Confirm badge updates still work correctly
    - Confirm color schemes are still consistent
    - Confirm all tests still pass after fix (no regressions)
    - _Requirements: 3.1, 3.2, 3.3, 3.4, 3.5_

- [x] 4. Checkpoint - Ensure all tests pass
  - Ensure all tests pass, ask the user if questions arise.
